package com.pleon.buyt;

import android.os.AsyncTask;

/* TODO: what happens if a configuration change (e.g. device rotation) happens OR
 * user presses the home button OR
 * application exits OR
 * application exits and is removed from recent apps?
 */
public class LocationTask extends AsyncTask {

    @Override
    protected Object doInBackground(Object[] objects) {
        return null;
    }
}
