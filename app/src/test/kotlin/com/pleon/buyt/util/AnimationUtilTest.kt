package com.pleon.buyt.util

import android.graphics.drawable.Animatable
import android.graphics.drawable.AnimatedVectorDrawable
import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.AfterEach
import org.junit.jupiter.api.BeforeEach
import org.junit.jupiter.api.Test

internal class AnimationUtilTest {

    // Use instrumented tests with Robolectric and mockito instead
    @Test
    fun `animate an icon infinitely`() {
        // val drawable = AnimatedVectorDrawable()
        // animateIconInfinitely(drawable, 0, 0)
        // assertThat((drawable as Animatable).isRunning).isTrue()
    }

}
